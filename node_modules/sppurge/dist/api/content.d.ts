import { IContext } from '../interfaces';
import { IContent } from '../interfaces/content';
export declare class Content {
    private spr;
    private agent;
    constructor();
    getFolderContent(context: IContext, spRootFolder: string): Promise<IContent>;
    private getCachedRequest;
}
