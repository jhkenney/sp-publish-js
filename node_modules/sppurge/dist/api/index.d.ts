import { IContext } from '../interfaces';
export declare class Delete {
    private spr;
    deleteFile(context: IContext, filePath: string): Promise<any>;
    deleteFolder(context: IContext, folderPath: string): Promise<any>;
    private deleteRequest;
    private getCachedRequest;
}
